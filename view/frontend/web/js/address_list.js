/*
 *
 * Copyright (c) 2017. @pablo
 *
 * Test Code
 */

require([
    'jquery'
], function($) {
    'use strict';


     $(function() {
        // todo: this view is not used by now
       /*
        setTimeout(function () {
             var zone_data =  JSON.parse($('#zone_data').html());

             zone_data.forEach(function(value){

                 // get div id and put data before
                 $('a[href="' + decodeURIComponent(value['href']) + '"]').parent().before('<div class="zone_data"><hr>' + decodeURIComponent(value['zone_data']) + '</div><br>');

             });
         },3000);
        */
        });



});